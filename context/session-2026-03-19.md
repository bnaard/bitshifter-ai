# Session Log: 2026-03-19

## What happened
Page-by-page review of the overview document ("Beyond Multiply-Accumulate", 53 pages).
Reviewed pages 6-16 in detail with Bernhard, addressing his feedback.

## Changes made (commit 6864264)

### Introduction (Section 1)
- Added paragraph acknowledging quantization (GPTQ, AWQ, 4-bit, 2-bit) as existing optimization
- Nuanced energy bullet: now shows FP32 → INT8 → bitwise progression with specific pJ numbers
- Nuanced deployment bullet: now mentions 4-bit quantized sizes alongside binary network sizes
- Added "bitwise neural computation" phrasing before paradigm diagram
- Fixed paradigm diagram: arrow labels now horizontal, positioned near boxes, not overlapping circle
- Expanded MNIST on first use in TM item
- Expanded LUT on first use in KAN item
- Added cross-references from each paradigm item to its detailed section
- Expanded §1.3 Scope to reference Sections 8-10

### Foundations (Section 2)
- Fixed gate diagram: both input arrows (1 and 0) now enter the gate boxes
- Energy table: added INT8 add/multiply rows; caption explains 45nm vs modern nodes (5-10x lower absolute, same ratios)
- Added Hamming distance/similarity footnote with formula
- MNIST digit now rendered in grayscale (was blue)
- Fixed "784 inputs" / "Output" column header overlap in MNIST figure
- Training section: clarified supervised learning, labeled datasets, loss definition
- Engineer's note: explained chain rule (why differentiability needed), defined reinforcement learning
- Added forward cross-references to paradigm sections

### Binary Neural Networks (Section 3)
- Added explanation of why XNOR and not XOR (similarity vs dissimilarity)
- Expanded STE explanation: explains true derivative is 0, STE replaces with 1
- Redesigned STE figure: clear separation of forward/backward pass, labels don't overlap
- Math summary: explained W, x, dimensions; derived 2*popcount-m step by step; defined batch normalisation
- Added forward ref to BitNet reality check in §10

### Tsetlin Machines (Section 4)
- Added "Open question" on dynamic clause growth (clauses are fixed hyperparameter, automata evolve content, no published dynamic growth variant)

### Cross-references (Sections 3, 9)
- §9: RMSNorm mention → forward ref to §10 integer normalisation
- §9: BitNet mention in attention key insight → forward ref to §10 analysis
- §3: BNN performance table → forward ref to §10 BitNet reality

### New content
- Abbreviation table added at end of document (99_abbreviations.tex)
- 4 new bibliography entries: Stillmaker 2017, GPTQ 2022, AWQ 2023, Convolutional TM 2019
- Downloaded GPTQ and AWQ papers to research/papers/

### DevContainer
- Restored latex-workshop settings (recipes, tools, outDir, synctex) that dev-box generate overwrote
- Added dev-box.toml to repo

## Still pending (for future sessions)
- Continue page-by-page review from page 16 onwards (Section 4: Tsetlin Machines onward)
- File dev-box issues:
  1. Missing TeX packages in rust-latex image: algorithms, algorithmicx, tikzfill
  2. dev-box generate strips latex-workshop compilation profiles from devcontainer.json
- Full abbreviation first-use audit (table exists, but not all first uses verified in later sections)
- Stillmaker 2017 paper not downloadable (paywalled Elsevier journal)

## Key decisions
- Energy argument now positioned as: quantization helps a lot, but bitwise goes further (INT8 mul still 2x bitwise, FP32 mul 37x)
- Horowitz 2014 45nm data kept but with caveat that ratios hold across process nodes (architectural, not process-dependent)
- TM clause count is fixed hyperparameter — noted as open research question
